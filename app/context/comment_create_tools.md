### COMMENT — CREATE TOOLS

**add_comment(work_item_id, comment, created_by, comment_reply_id)**
Add a comment or threaded reply to a work item.
- `comment_reply_id` is optional; provide to reply to an existing comment.
- Use current user UUID for `created_by`.
