### PROJECT — CREATE TOOLS

**create_project(title, managed_by, created_by, description, status)**
Create a new project.
- `managed_by` and `created_by`: use current user's UUID unless told otherwise.
- `status` defaults to `OPEN`.
