### PROJECT — UPDATE TOOLS

**update_project(user_id, project_id, title, description, status)**
Update project title, description, or status. All fields except `user_id` and `project_id` are optional.
